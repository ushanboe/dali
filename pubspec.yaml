name: dali
description: Procedural graphics engine for Flutter — generate characters, scenes, and visual assets entirely in code. No image files required.
version: 0.1.0
homepage: https://github.com/bobbotboe/dali

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
